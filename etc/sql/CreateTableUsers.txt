CREATE TABLE IF NOT EXISTS USERS(
  id INT PRIMARY KEY AUTO_INCREMENT,
  username NVARCHAR(50) NOT NULL,
  password NVARCHAR(50) NOT NULL,
  email NVARCHAR(50)
);

INSERT INTO USERS (username, password, email) VALUES ('doublem', 'pass', 'doublem513@gmail.com');

SELECT * FROM USERS;

UPDATE USERS SET
  username='oktopid',
  password='8pidForPass',
  email='okto@pid.org'
WHERE id=1;

CREATE TABLE IF NOT EXISTS DICTIONARY_ENTRIES(
  id INT PRIMARY KEY AUTO_INCREMENT,
  english NVARCHAR(200),
  croatian NVARCHAR(200)
);

CREATE INDEX IF NOT EXISTS DICTIONARY_INDEX ON DICTIONARY_ENTRIES(english, croatian);

SELECT * FROM DICTIONARY_ENTRIES LIMIT 100;

SELECT count(*) FROM DICTIONARY_ENTRIES;